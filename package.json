{
  "name": "my-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add docs/ && git commit -m \"deploy: update site\" && git push origin master"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
